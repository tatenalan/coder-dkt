    <nav class="navbar navbar-expand-lg navbar-light bg-dark fixed-top">
        <a class="navbar-brand text-white" href="/"><img src="/img/logo-horizontal-png.png" style="width:100px;" alt="Logo Dakota"></a>
        <button class="navbar-toggler" style="color:white;" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarText">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link text-white" href="/products">Products</a>
                </li>
            </ul>
            
            {{#if username }}
                <a class="nav-link navbar-text text-white" href="/profile">Profile</a>
                <a class="nav-link text-white" href="/chat">Chat</a>
                <a class="nav-link navbar-text text-white" href="/orders">Orders</a>
                <a class="nav-link navbar-text text-white" href="/cart">Cart</a>
                <form action="/logout" method="post">
                    <button type="submit" class="btn btn-dark">Logout</button>
                </form>
            {{else}}
                <a class="nav-link navbar-text text-white" href="/login">Login</a>
                <a class="nav-link navbar-text text-white" href="/register">Register</a>
            {{/if}}
        </div>
    </nav>
    {{#if username}}
    <div class="alert alert-dark mb-5 d-flex align-items-center justify-content-between" role="alert">
        Welcome {{username}}!
    </div>
    {{/if}}
    
    
    
    
    {{!-- <div class="alert alert-dark d-flex align-items-center justify-content-between" role="alert">
        {{#if username}}
        <a class="btn btn-secondary" href="/products/">Products</a>
        <a class="btn btn-secondary" href="/chat/">Group Chat</a>
        <a class="btn btn-secondary" href="/cart">Cart</a>
        <a class="btn btn-secondary" href="/profile">Profile</a>
        <form action="/logout" method="post">
            <button type="submit" class="btn btn-secondary">Log out</button>
        </form>
        {{else}}
        <div class="alert mt-4 d-flex align-items-center justify-content-end" role="alert">
            <a class="btn btn-secondary" href="/login">Login</a>
        </div>
        {{/if}}
    </div>
    {{#if username}}
    <div class="alert alert-dark mb-5 d-flex align-items-center justify-content-between" role="alert">
        Welcome {{username}}!
    </div>
    {{/if}} --}}